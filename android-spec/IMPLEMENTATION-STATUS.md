# Android implementation status (living document)

**Purpose:** Track what is **done**, **in progress**, or **not started** in this repo relative to `android-spec/`. Update this file when milestones land.

**Roadmap:** [PHASES-AND-FEATURES.md](PHASES-AND-FEATURES.md) · **Coverage:** [FEATURE-COVERAGE-MATRIX.md](FEATURE-COVERAGE-MATRIX.md)

---

## How to update

1. When a spec slice or phase gate is satisfied in code, tick **`[x]`** and add a short **Note** (PR, area, or follow-up).
2. Deferred work: mark **Deferred** in Notes with a link or reason.
3. Set **Current phase** and **Last updated** when you edit this file.

---

## Current focus

| Field | Value |
|-------|--------|
| **Current phase** | **Phases 4 / 5 / 6 / 7 — Complete**. Phase 7 closeouts: spec [12] (legal sheet + support form deep-links + Settings→Terms wiring + `SessionAuthSyncObserver` post-login bootstrap → activity-log user + home-city detect); RouteActivity dropdown ports `CarrierExploreDropdownState` (4-method object + 6 JVM tests, iOS thresholds preserved) and surfaces `newPackagesThisWeekNearHome` on `RouteActivitySummary` so the carrier-explore search field overlays a Recent / Popular dropdown only when focused + empty + at least one section qualifies. Content-mode switching now lands `CarrierExploreContentMode` (Nearby / Recent / PopularList / PopularDestination / Search) end-to-end — Recent injects `new_this_week=true&sort_by=created_at&sort_order=desc` on `PackageBrowseFilter` to share the existing `/packages/available` paginator, matching iOS `CarrierBrowseContentMode.recent`. |
| **Last updated** | 2026-05-14 — **BadgeSummary single source-of-truth (9 slices)** — closes the "bell shows 2 but drawer empty" drift on iOS parity. New `GET /me/badge-summary` is now the only attention source feeding the tab-bar Profile badge, dashboard bell, drawer Action Required / Account Setup / Pending Reviews sections, and Profile-row Payout / Pending-reviews badges. **Slice 1** — `BadgeSummaryJson` (+ nested `ActionRequiredJson` / `VerificationJson`) in `:core:network` with snake_case + defaults; `BadgeSummary` domain + `toDomain()` mapper that parses `computed_at` to `Instant?` tolerantly. 12 + 7 decoder/mapper tests. **Slice 2** — `BadgeSummaryRepository` (singleton; per-role `Mutex.tryLock` coalescing — second concurrent call is a no-op; failures preserve last-good summary + flip `hasValidData=false`; `snapshotMode` + `seedForSnapshotTesting`; token guard via `TokenStore`). `BadgeRefreshBus` `MutableSharedFlow<Unit>`. `BadgeSummaryLifecycleObserver` started from `PasabayanApplication.onCreate` wires `ProcessLifecycleOwner` ON_START (via `repeatOnLifecycle`), `AuthRepository.currentUser()` (distinct on id + phoneVerified), and the bus to repo refresh/clear. New `androidx-lifecycle-process` + `lifecycle-runtime-testing` deps. 12 + 3 + 7 unit tests across repo / bus / observer. **Slice 3** — `FcmBadgeRefreshDispatcher` extracted from `PasabayanFirebaseMessagingService`: `data.type == "badge_refresh"` silent push emits to the bus and short-circuits notification display; logs `reason` for support correlation. 5 tests. **Slice 4** — new `BadgeSummaryViewModel` (thin Hilt VM); `MainTabScreen` swaps Profile-tab badge source from `ProfileAttentionViewModel.total` to `summary.total` gated on `hasValidData` (matches iOS `applyBadge(hasValidData ? summary.total : 0)`). `LaunchedEffect(currentRole)` triggers per-role refresh. 3 VM tests. **Slice 5** — bell badge swaps from `notificationState.unreadCount` to the same gated `BadgeSummary.total`; bell + tab-badge now share a single local val and can never drift. **Slice 6** (the regression-fix slice) — `ComprehensiveNotificationsScreen` adds a fourth **Account Setup** section between Action Required and Pending Reviews, visible only when populated. Cards: `Verify Your Phone` (shield-check icon, red accent) and `Set Up Payouts` (credit-card icon, green accent), tapping presents `PhoneVerificationSheet` / `PayoutSetupScreen` directly from the drawer (no tab switch — matches iOS). `buildActionableItemSpecs` rewritten as a pure mapper from `BadgeSummary.actionRequired` (per-role split: carrier sees BOOKING_REQUEST / STATUS_UPDATE / INACTIVE_TRIPS, shipper sees CARRIER_RESPONSE / PICKUP_READY); new `buildAccountSetupSpecs` mirrors `verification.*`. Local match/trip/package state no longer drives visibility — only routes the tap. 16 aggregator tests + 5 drawer Compose preview fixtures (all light + dark) including the account-setup-only regression fixture. EN+FR strings; legacy `_verify_number_` / `_upgrade_premium_` / `_unread_messages_` keys removed. **Slice 7** — `ProfileTabScreen` swaps `ProfileAttentionViewModel` for `BadgeSummaryViewModel`; `ProfileTabContent` parameter changes from `attention: AttentionSignalsJson` to two scalars (`payoutSetupNeeded`, `pendingReviewsCount`) to decouple the screen from the legacy wire DTO. **Slice 8** — `StripeConnectViewModel.handleOnboardingReturn`, `RatingsViewModel.saveComment().onSuccess`, and `PhoneVerificationViewModel.verifyOtp().onSuccess` each post to `BadgeRefreshBus`. 6 new mockk-verify tests (success path emits, failure path does not). **Slice 9** — retired legacy `/me/attention` plumbing: deleted `ProfileAttentionViewModel`, `ProfileAttentionRepository`(+Impl), `AttentionSignalsJson`, both test files; removed `getAttention()` from `ProfileApi` and the Hilt binding. **Plus prior context: 2026-05-13 — Trips parity sweep (7 slices: A–G)** — closes every gap in [TRIPS-PARITY-REVIEW.md](TRIPS-PARITY-REVIEW.md). **Slice A** — new `POST /trips/{id}/activate` on `TripsApi` + `TripsRepository.activateTrip` + `CarrierTripsViewModel.activateTrip` / `suspendActivateTrip`; `CarrierMyTripsScreen` Activate dialog now routes through the sanctioned endpoint instead of the silently-dropped `PUT /trips/{id}` with `trip_status=active`. **Slices B1–B6** — `EditTripSheet` expanded from 161 LOC (weight + notes) to a section-based form mirroring iOS' 1,593 LOC sheet: new `components/EditTripRouteSection` / `EditTripScheduleSection` / `EditTripCapacitySection` / `EditTripPricingSection` / `EditTripStatusSection`, `TripFormState` carries `originCountry` / `destinationCountry` / `sharedPickupDateMillis` / `sharedDeliveryDateMillis`, `TripFormValidator` enforces iOS-parity bounds (`MAX_WEIGHT_KG=2000`, `MAX_SPACE_LITERS=5000`, `MAX_PRICE_PER_KG=100`) with new `WeightOutOfRange` / `SpaceOutOfRange` / `PricePerKgOutOfRange` errors, `onSave` switched from `(weight, notes)` to a partial `TripUpdateRequestJson` that only emits changed fields and drops route/schedule when the trip has left planning (`canEditRoute` policy parity), in-sheet Activate confirmation + destructive Cancel Trip flow with iOS-parity HTTP 409 → new `DomainError.TripHasBlockingMatch` mapping that joins server message with `trips_details_cancel_error_match_in_progress_hint`; `DomainErrorMessages` + `ErrorAlertPolicy` exhaustive-when branches added. **Slice C** — `CreateTripFromPackageViewModel` adds `requestToCarryMessage: String?` and routes iOS' error phrases (`request to carry` / `compatible trip` / `has compatible trips` / `already have`+`trip`, case-insensitive) into the alert state instead of generic `errorMessage`; `CreateTripFromPackageScreen` renders the request-to-carry `AlertDialog` (primary "Request to carry" + secondary Cancel) and exposes a new `onRequestToCarry(packageId)` callback; `clearCreatedTrip` doc-commented as one-shot consumer (not a form reset). **Slice D** — verification: exhaustive iOS search confirmed `passengerCapacity` / `pricePerPassenger` / `passengerRequirements` / `ageRestrictions` / `passengerAmenities` are dormant `Trip.swift` decode-only fields (no UI binds them, no `CreateTripRequest` / `TripUpdateRequest` carries them on either platform); §5 closed in `TRIPS-PARITY-REVIEW.md` with the evidence inline. **Slice E** — `nextStatusOptions` now opens `CANCELLED` from `PLANNING` + `ACTIVE` matching iOS' `statusPickerOptions:228`; `TripStatusUpdateSheet` gains `onActivate` + `onCancel` callbacks, dispatches by target with iOS-parity confirmation alerts (activate confirm + destructive cancel/keep-trip) and reuses Slice B6 blocking-match formatting; `TripDetailsScreen` plumbs both callbacks through; `CarrierMyTripsScreen` + `MainTabScreen` wire to `suspendActivateTrip` / `suspendCancelTrip`. **Slice F** — new reusable `components/TripPricingBreakdown` composable renders `basePrice` + `distance` + `distance multiplier` + `calculatedPrice` as a tinted block under the headline price in `TripDetailsScreen` when the server returns the breakdown; `CarrierEarningsSection` audited and already iOS-parity (no code change). **Slice G** — `TripTemplateJson` gains `suggested_transportation_method`, `CreateTripFromPackageRequest` carries `sharedPickupDate` / `sharedDeliveryDate`, `TripsRepositoryImpl.createTripFromPackage` forwards both onto wire `pickup_date` / `delivery_date`; `CreateTripFromPackageScreen` auto-preselects transport via new `parseTransportationMethod` helper and auto-derives shared pickup/delivery from departure/arrival in `buildRequestFromState` — closes iOS `populateFormFromTemplate:202-217` + `syncCarrierLegFromSharedSchedule`. **Tests** across the sweep: `TripsRepositoryImplTest` (activate path/method/empty-body + 4xx + network failure; cancel 409 → TripHasBlockingMatch with server message; createTripFromPackage wire `pickup_date` / `delivery_date` present-when-set / omitted-when-null), `EditTripSheetRequestTest` (per-section emission + locked-trip drops + unchanged-field suppression + snake_case wire keys), `TripFormValidatorTest` (boundary tests for 2000 kg / 5000 L / $100/kg, plus land flat doesn't trip per-kg cap), `CarrierTripsViewModelTest` (activate happy/failure/non-planning + suspend variants; `editTrip` partial-body propagation; `suspendCancelTrip` success + skips local pre-flight + invalid-transition + repo-failure-doesn't-flip-state), `CreateTripFromPackageViewModelTest` (5 request-to-carry phrase tests covering case-insensitivity + transport field preservation), `CreateTripFromPackageRequestBuilderTest` (shared-schedule defaults to departure/arrival + blank-leaves-null + `parseTransportationMethod` happy/blank/unknown), `TripJsonModelsDecodeTest` (suggested_transportation_method decode + null fallback), `TripDetailsSectionsModelTest` (transition matrix flipped — planning → active+cancelled, active → in_transit+cancelled, in_transit forward-only). EN+FR strings added across the sweep (`trips_action_activate_trip*` reused, plus `trips_edit_shared_pickup` / `_delivery` / `trips_edit_capacity_section` / `trips_edit_pricing_section` / `trips_edit_status_section` / `trips_edit_status_locked_note` / `trips_create_validation_weight_out_of_range` / `trips_create_validation_space_out_of_range` / `trips_create_validation_price_per_kg_out_of_range` / `trips_details_cancel_error_match_in_progress_hint` / `trips_edit_cancel_error_generic` / `trips_from_package_alert_trip_exists_title` / `trips_from_package_alert_request_to_carry` / `trips_status_update_hint_cancelled` / `trips_detail_pricing_base_label` / `_distance_label` / `_multiplier_label` / `_calculated_label` / `trips_detail_distance_km_format` / `trips_detail_multiplier_format`). **Plus prior context: Advisory-weight policy sweep (6 slices)** — Android caught up to the iOS / backend contract on over-capacity matches. Weight is now advisory at request time and only enforced at accept time via an explicit `acknowledge_overage` flag. **Slice 1**: `BookingsApi.shipperAccept` renamed to `shipperAcceptCarrierRequest` and switched from `POST /matches/{id}/accept` to canonical `PUT /matches/{id}/accept-carrier-request`; new `AcceptMatchRequestJson` body (`message`, `acknowledge_overage`); both repository accept methods take `acknowledgeOverage: Boolean? = null` (omitted from JSON when null). **Slice 2**: `MatchCompatibilityJson` decoded as a sibling of `data` on both request envelopes (`weight_over_capacity`, `package_weight_kg`, `trip_available_weight_kg`, `overage_kg`, `dates_misaligned`, `route_uncertain`, `requires_capacity_acknowledgment` — note the renamed field); new `MatchCompatibility` domain model on `RequestMatchResult.compatibility`; stale `warnings: ["over_capacity"]` fixture removed (the policy moves weight out of `warnings[]` into the structured object). **Slice 3**: `DomainError.CapacityAcknowledgmentRequired(message, packageKg, tripAvailableKg, overageKg)` + `DomainError.TripOvercommitted(message)`; `ApiErrorMapper.map422` branches on `error == "capacity_acknowledgment_required"` and parses the inline compatibility kg; `map409` branches on "fully booked" / "no remaining capacity"; `ErrorAlertPolicy` classifies both as foreground-only; new `JsonObject.doubleAt` helper; localized fallback strings (en + fr). **Slice 4**: `MatchingUiState.pendingOverageConfirmation: OverageConfirmationData?` + `tripOvercommitted: String?`; `acceptMatch` runs a local pre-flight (`packageWeightKg > availableWeightKg`) and surfaces the sheet state instead of hitting the network; `confirmOverageAcceptance()` retries accept with `acknowledge_overage=true`; `dismissOverageConfirmation()` and `clearTripOvercommitted()` clear state; failure handler maps `CapacityAcknowledgmentRequired` (defense in depth using server kg values, falling back to local) into the same sheet, and `TripOvercommitted` into the dedicated state. **Slice 5**: new `features/bookings/components/OverCapacityBanner.kt` (amber-tinted `Surface` over `PasabayanColors.Warning`, 1 dp tinted border, theme-aware foreground text, 4 light + dark previews); new `features/bookings/components/OverageConfirmationDialog.kt` wrapping `PAlertDialog` with `WarningAmber` icon and role-specific carrier/shipper body copy (4 previews); banner mounted in `ShipperMatchDetailsSheetContent` above action buttons whenever the match is over-capacity (computed from the match record so it persists across reopens — matches iOS rationale); dialog bound in `MatchListScreen` to `state.pendingOverageConfirmation`; `state.tripOvercommitted` surfaces as a dedicated `PAlertDialog`. 11 new strings (en + fr): `matching_overage_banner_title/body`, `matching_overage_confirm_title/body_carrier/body_shipper/accept/cancel`, `matching_overage_kg_format`, `matching_trip_overcommitted_title/dismiss`. **Slice 6**: `DeprecationLoggingInterceptor` + `DeprecationLogger` fun interface + `AndroidDeprecationLogger` (debug-only `Log.w` gated by `BuildConfig.DEBUG`); installed in OkHttp builder between the unauthorized-clearing and body loggers; 3 JVM tests (header `true`, absent, `false`); `okhttp-mockwebserver` added to `:core:network` test deps. **Behavior summary**: counter-offer / request flows never block on weight — server always accepts; accept flow gates locally on pre-flight OR on the 422 fallback, retries with `acknowledge_overage=true` on user confirm; Counter-Offer CTA visibility still keys only on the server's `canCounterOffer` (Android does not hide it on overage); legacy `POST /trips/{tripId}/packages/{packageId}/accept` is no longer called and any 200 with `Deprecation: true` logs a debug warning. Spec [05-bookings-matches.md](05-bookings-matches.md) gains a new "Advisory-weight policy" section + TDD rows. **Plus prior context: Carrier-explore Recent mode** (iOS parity, no backend probe needed): `PackageBrowseFilter` gains a `recent: Boolean` field that, when true, injects `new_this_week=true&sort_by=created_at&sort_order=desc` into `toQueryMap()` — matching iOS `CarrierBrowseContentMode.recent` in `CarrierHomeContent.swift:1824`. The dropdown's Recent tap now sets the flag + reapplies the filter (instead of showing a "coming soon" card); the existing `/packages/available` paginator handles fetch/render/load-more for free. Recent mode renders the same `CarrierExplorePackageCard` list as Nearby with a "Recent Packages Near You" header above + Back-to-Nearby affordance. `returnToNearby` now also clears the recent flag so toggling out is symmetric. 2 new JVM tests in `PackageBrowseFilterTest` pin the recent wire contract (alone + layered on top of user filters). 1 new EN+FR string (`dashboard_carrier_explore_recent_header`); the deferred `dashboard_carrier_explore_recent_coming_soon` placeholder removed. Closes the last behavior delta on carrier-explore content-mode switching. **Plus prior context: Explore-tab final pass + `UserProfilePopover` parity (6 slices)** landed on top of the day's earlier explore-tab parity. **Search IME action** (both roles): `POutlinedTextField` on `CarrierExploreContent` and `ShipperExploreContent` wire `KeyboardOptions(imeAction = ImeAction.Search)` + `KeyboardActions(onSearch = ...)` to submit on Enter; iOS already did this. **Empty-state CTA navigation** (both roles): `onNavigateToMyTrips` / `onNavigateToPackages` callbacks plumbed from explore screens up to `MainTabScreen`, which routes to tab index 2 (role-specific) via `viewModel.selectTab(2)`. iOS leaves these CTAs unwired, so Android is now strictly better. **Nearby Carriers section** (shipper-explore): new `features/shipper/viewmodel/NearbyCarriersViewModel.kt` (`loadNearbyCarriersIfNeeded` / `loadNearbyCarriers` / `resetForNewSession`; `carriersWithCompletedDeliveries` computed property mirroring iOS `carriersWithCompletedTrips`) + `features/shipper/components/NearbyCarriersSection.kt` (horizontal "Top Carriers" chip row, `Person` glyph fallback avatar — image loading deferred until Coil/Glide lands). Mounted in `ShipperExploreContent` `LazyColumn` above the popular-routes block. 6 new JVM tests + 3 EN+FR strings. **`UserProfilePopover` parity (Slices A/B/C)** closes the largest iOS-vs-Android delta on explore: trip cards on shipper-explore had no carrier-header row and there was no way to view another user's profile. **Slice A** — new `features/dashboard/components/UserCardHeader.kt` reusable composable (avatar initials + name + verification dot + star/rating row + chevron + `Modifier.clickable` wrap when tappable). `TripCard.kt` mounts it at the top of both collapsed and expanded views when `trip.carrier != null`. `onOpenCarrierProfile: (() -> Unit)? = null` plumbed through `TripCard` → `ShipperExploreTripCard`. **Slice B** — new `features/profile/viewmodel/UserProfilePopoverViewModel.kt` + `features/profile/ui/UserProfilePopover.kt` mirroring iOS `UserProfilePopover.swift`: 80 dp avatar with `CheckCircle` verification overlay, verification pill, member-since label, star/rating + (X trips) for carriers or role tag for shippers; Performance stats card (Deliveries / Earned / Rating) gated by `viewerIsSelf == true` so Android **side-steps the iOS bug** where `loadCarrierStats()` shows your own stats when viewing someone else; Rating-distribution histogram (5-row gold-on-gray bars via `BoxWithConstraints`); paginated `UserReviewCard` list with "Load more" `PButton`; shipper-only "Add to Favorites" toggle with optimistic flip + revert-on-failure. VM wires `RatingsRepository.fetchReceivedRatings(userId)`, `FavoritesRepository.isFavorite/add/remove`, `ProfileRepository.fetchCarrierStats`. 23 new EN+FR strings (verification pill / performance / favorites / no-reviews copy); 9 JVM tests covering load gating, pagination append + dedupe, no-op guards, distribution math, optimistic+revert favorite, error surfacing. **Slice C** — `ShipperExploreContent` and `CarrierExploreContent` host `UserSummary?` sheet-state vars and mount `UserProfilePopover` when non-null. Trip card opens it with `userRole = CARRIER` + `currentUserRole = SHIPPER` (favorite button visible); package card opens it with `userRole = SHIPPER` + `currentUserRole = CARRIER` (favorite hidden — shippers favorite carriers, not the reverse). **Bug-fix**: `CarrierExplorePackageCard`'s private `ShipperHeader` set `Modifier.padding` when `onOpenProfile != null` but **never `.clickable`**, so the carrier-side tap silently dropped. Swapped to the shared `UserCardHeader` from Slice A (handles clickable + chevron correctly); private `ShipperHeader / AvatarInitials / VerificationDot / RatingRow` deleted. **Behavior deltas vs iOS**: (a) `memberSince` left `null` everywhere — `UserSummary` doesn't carry the field yet, so the row hides until `created_at` is plumbed; (b) avatar uses initials fallback until Coil/Glide lands as a cross-cutting effort. **Plus Explore-tab parity with iOS (5 slices)** landed on top of the day's earlier work. **Slice 1 — carrier-side filter model + VM pagination**: new `features/packages/model/PackageBrowseFilter.kt` mirrors iOS `PackageBrowseFilter` (`searchText` → `q`, `urgency` → `urgency`, `maxWeight` → `max_weight`, `maxPrice` → `max_budget`, `packageType` client-side only); new `features/packages/model/AvailablePackagesPage.kt` envelope (`packages`, `currentPage`, `lastPage`, `total`, `perPage`, `nearby`) with `hasMore = currentPage < lastPage`. `PackagesRepository.loadAvailablePackagesPage(filter, page, perPage = 15)` decodes the full Laravel envelope + top-level `nearby` flag (DTO already carried both). `PackageViewModel` extended with `availablePackagesFilter / CurrentPage / HasMore / IsLoadingMore / Nearby / LoadMoreError`, private `loadGeneration` race guard, `visibleAvailablePackages` computed property (client-side `packageType` filter), and methods `setBrowseFilter / applyBrowseFilter / clearBrowseFilter / loadMoreAvailablePackages` (dedupes appends by `effectiveId`). 9 new VM tests + 11 filter-wire-contract tests. **Slice 2 — filter sheet UI + LazyColumn auto-load**: new `features/packages/components/PackageFilterContent.kt` (urgency + package-type chip rows + max-weight + max-price decimal inputs + Clear/Apply) and `features/packages/ui/PackageFilterSheet.kt` (mirrors `TripFilterSheet`). `CarrierExploreContent` rewritten on `LazyColumn` with `derivedStateOf { lastVisible >= total - 3 }` → `snapshotFlow().distinctUntilChanged().collect { loadMore() }` auto-paginate; structured search submit + Tune `IconButton` opens the sheet; legacy ad-hoc `Map<String,String>` search path retired from production. 14 new EN+FR strings under "Carrier explore filters". **Slice 3 — shipper auto-paginate + race guard**: new `features/trips/model/AvailableTripsPage.kt` envelope; `TripsRepository.loadAvailableTripsPage(filter, page, perPage = 15)` surfaces the same currentPage/lastPage envelope. `BrowseTripsViewModel` rewrites `loadAvailableTrips()` + `loadMoreTrips()` through a `loadGeneration` race guard and a private `applyPage(page, isReset)` helper that sets `hasMore = page.hasMore` — replaces the brittle `hasMore = filtered.isNotEmpty()` heuristic that mis-fired when the client-side `BOOKABLE_STATUSES` filter emptied a non-final page. `ShipperExploreContent` drops the manual "Load More" `PButton` for the same end-of-list `LaunchedEffect` pattern carrier-explore uses; `PopularRouteCard` extracted as a private composable. 4 new JVM tests (no-op when no more / no-op before first page / reset discards stale / envelope-driven hasMore); 5 existing tests migrated to the new page-queue path. `FakeTripsRepository` (shared) gains `tripsPageResultQueue` + `tripsPageCalls`; private fake in `CreateTripFromPackageUseCaseTest` extended to satisfy the new method. **Slice 4 — NearbyFallbackBanner**: new `features/packages/components/NearbyBanner.kt` — amber `LocationOff` row that renders only when `availablePackagesNearby == false` ("No packages nearby — showing from other areas"). Mounted in `CarrierExploreContent` above the package list. **Android closes the iOS gap** where `CarrierBrowsePackagesView.swift:35-37` references a `nearbyBanner` that's never implemented. Reuses the long-existing `dashboard_carrier_not_nearby` EN+FR string. **Slice 5 — spec sync**: `04-packages.md` gains an "Android browse-pagination contract" section documenting the filter wire keys, envelope domain model, hasMore semantics, VM state machine, race guard, end-of-list auto-paginate UX, and the iOS dead-code note. `03-trips.md` gains the symmetric section noting that `/trips/available` carries no `nearby` flag (server-side asymmetry, not an Android gap). **Behavior delta vs iOS**: identical browse contract on both platforms; Android auto-paginate UX is now strictly an improvement over iOS (which already auto-paginated; the gap was on the Android side). **Plus Spec [09](09-profile-carrier-consent.md) TDD gate closed (4 slices)**: TDD checklist (lines 580-593) reconciled with reality — 10 of 12 rows ticked with `→ {actual test file}` annotations (work was shipped, doc had lagged); 2 genuine gaps filled. New `core/network/.../profile/CarrierStatsDecodingTest.kt` (14 cases) pins `FlexibleDoubleSerializer` for `success_rate`, `response_time_hours`, all earnings fields, and `potential_earnings` on both numeric and stringified shapes; covers all-null sub-models, missing `top_routes`, and `CarrierStatusDataJson.userTypes` decoding via `UserTypesSerializer` for both array form (`["shipper","carrier"]`), index-keyed dict (`{"0":"shipper"}`), and key-flag dict (`{"shipper":true}`); cross-checks `UserSummary` shares the same serializer. New `core/domain/.../enum/VerificationLevelTest.kt` (11 cases) pins `normalized()` (recognized lowercase / case-insensitive / trim / null / empty / unknown → BASIC), the `isVerified` / `isPremium` matrix, and `@SerialName` round-trip per level so a future API rename can't silently break decoding. Badge text/color stays an `androidTest` follow-up documented inline on the spec row. Plus the day's earlier **Counter-offer UI polish + validation parity (6 slices)** landed on top of the day's earlier work. **Slice 1 — model**: `CounterOfferContext` gains `isLowerOffer` / `isHigherOffer` / `formattedNewPrice` / `formattedOriginalPrice` / `formattedDifference` / `summaryKind` (`CounterOfferSummaryKind { LOWER, HIGHER, UNCHANGED }`), companion factories `fromMatch(match)` and `fromNotificationData(data, fallbackName)`, and an internal `originalPriceFromMessage(match)` regex (`(?:\(\s*was\s*\$?\s*X\s*\)|\bwas\s*\$?\s*X\b)`) that recovers the original price from chat-message hints. `DeliveryMatch` now carries `counterOffererId` / `counterOffererName` (the DTO already had them — they were being dropped at `BookingMapper`). 17 new JVM tests in `CounterOfferContextTest`; 2 added in `BookingMapperTest`. **Slice 2 — banner**: `CounterOfferBanner` rewritten from a 1-line stub to the iOS-parity layout — header (purple `$` icon + title + "from {name}" + dismiss) · 3-column was/diff/now price comparison · role-aware summary ("You saved", "Increased by", "You'll earn $X more", "$X below your rate", generic "Reduced by") · metadata lines (round, initiated-by, remaining count / limit reached). Renders "You" when `currentUserId == counterOffererId`. Strict DS tokens; 4 light+dark previews; `BANNER_TEST_TAG` / `BANNER_DISMISS_TEST_TAG`. 13 new EN+FR strings under "Counter offer banner". **Slice 3 — banner wiring**: `ShipperMatchDetailsSheetContent` mounts the banner at the top of the scaffold body when `CounterOfferContext.fromMatch(match)` is non-null; accepts optional `currentUserId: Int?` (callers pass null today — plumbing `AuthRepository.currentUser()` end-to-end is a follow-up). Preview switched to a counter-offer fixture so the banner shows in IDE. **Slice 4 — prompt**: `CounterOfferPromptSheet` rebuilt to iOS parity — declined-price card (strikethrough), optional reference-price card (carrier rate / shipper budget), `$`-prefixed `KeyboardType.Decimal` input with min hint, sealed validation (positive, below `StripeConfig.minDeliveryPrice`, must-differ ±$0.005 tolerance), submit `isLoading` state, tertiary "No thanks" cancel; new pure `CounterOfferPromptValidator` extracted to `services/` with `sealed class CounterOfferPromptValidation { Empty, NotNumeric, NotPositive, BelowMinimum, SameAsOriginal, Valid }`. `MatchListScreen` now keeps the sheet open while submitting and a trailing-edge `LaunchedEffect(state.isSubmittingCounterOffer)` closes it on completion so the spinner is visible. 12 new JVM tests in `CounterOfferPromptValidatorTest`; 8 new EN+FR strings. **Slice 5 — snackbar**: new `CounterOfferSnackbar` (`PCard` shell, purple `$` icon, signed diff `-$X.XX` Success / `+$X.XX` Warning, "View Offer" + dismiss) mirrors iOS `CounterOfferSnackbar.swift`. Wired into `MatchListScreen` via `LaunchedEffect(state.lastNegotiation, state.matches)` that fires on successful counter-offer submission (`negotiation.isCounterOffer == true`) — the snackbar renders as the topmost `LazyColumn` item, "View Offer" opens the details sheet (which mounts the rebuilt banner), and a 6 s auto-dismiss `LaunchedEffect` clears it. 1 new EN+FR string (`bookings_counter_offer_view_offer`). **Slice 6 — error mapper**: `CounterOfferErrorMapper` adds explicit `ValidationError` (delegates to `DomainError.userMessage()` which humanizes Laravel field-error maps) and `ConsentRequired` (backend message verbatim, blank → DS fallback) branches plus a comment documenting the iOS-only `phoneVerificationRequired` gap (Android enforces phone verification client-side via `RequirePhoneVerificationUseCase`). 3 new JVM tests. **Behavior delta vs iOS**: currentUserId is not yet plumbed through `MatchingViewModel` / details sheet — banner and snackbar fall back to role-agnostic summary copy and never render "You" yet. Wiring `AuthRepository.currentUser()` through is a follow-up for the next session. **`IncomingRequestSnackbar` (5 slices, Android-only surface)** plus the day's earlier parity-fix work. **Snackbar slices**: `IncomingRequestContext` promoted from 2-field stub to a full surface model — companion helpers `from(match, isCarrier)` (role-scoped factory, rejects counter-offers / wrong-status / null requester party), `incomingRequestSnackbarItems(matches, isCarrier, reviewedIds)` (capped at `MAX_VISIBLE_ITEMS = 3`, preserves order), `dismissedIdsAfterReview(matches, isCarrier)` (candidate-id set for VM-side GC), and `unseenIncomingRequestBadgeCount(matches, isCarrier, reviewedIds)` (no cap — drives the Matches-tab badge). `MatchingUiState.reviewedIncomingRequestIds: Set<Int>` plus idempotent `MatchingViewModel.markIncomingRequestReviewed(matchId)`. New `IncomingRequestSnackbar` composable in `features/bookings/components/` built from `PCard` + DS tokens (no Material `Snackbar` — name predates the cards-stack decision); role-parameterised title, leading `Inventory2` icon tinted `BadgeBlue`, trailing close `IconButton`; test tags on root/title/dismiss; 4 light+dark previews. `MatchListScreen` integration adds the review section inside the `LazyColumn` above match items (max 3 + "+N more pending" caption when backlog exceeds the cap); empty-state guard widened to keep the snackbar visible when filtered matches are empty but incoming requests pend. Section is independent of the status filter. **Behavior delta vs iOS**: no iOS file mirrors this surface — iOS handles incoming requests inline in `CarrierRequestCard` / `BookingRequestCard` / `ShipperMatchCard` via status filters. Documented in `05-bookings-matches.md` § "Android-only surfaces"; TDD row ticked. 13 new JVM tests across `IncomingRequestContextTest` and `MatchingViewModelTest`. 4 new strings (en + fr) for snackbar titles, dismiss, and "+N more". See also: **Carrier-explore card & sheet parity, role-switcher consolidation, Messages-tab filter removal, system-message styling, and DS-enforcement rule** earlier today. **Carrier-explore parity**: new `CarrierExplorePackageCard` (`features/packages/components/`) mirrors iOS `CarrierPackageCard` content density — shipper header (avatar initials + name + verification dot + rating), title + package-type chip + `UrgencyBadge` + Fragile chip, `PRouteSection`, **Weight** + **Budget** (highlighted via new `PDetailRow.valueColor`), Pickup/Delivery schedule row, `PCardActionFooter` with inline "Request to Carry"; `UrgencyBadgeConfig` maps `UrgencyLevel → StatusBadgeConfig` (4 JVM tests). `CarrierPackageDetailSheet` (`features/packages/ui/`) rebuilt on `PDetailSheetScaffold` + `PDetailSheetCard` for 1:1 styling parity with shipper-explore `TripDetailsScreen` (5 iOS sections: Header, Route, Info, Schedule, Notes + "Request to Carry" CTA); reads `AvailablePackage` directly so it skips the shipper-scoped `GET /packages/{id}` (which 403s for non-owners). New `DashboardSheetRoute.CarrierPackageDetail` + `openCarrierPackageDetailSheet` + `PModalBottomSheet` host in `MainTabScreen`. **Role-switcher consolidation**: dropped the in-card 3-dot `RoleSwitcherIconButton` from `UserHeaderCard` and the `CompactRolePickerRow` from the profile header — both opened the same dropdown that `DashboardTopBar`'s `SwapHoriz` icon already provides. Static `RoleChip` remains for identification only. Also fixed a regression where `PExpandableCardHost` unmounted `MainTabScreen` during expansion → `LaunchedEffect(user) { initializeRole(user) }` re-ran on collapse → role flipped to shipper. `DashboardViewModel.initializeRole` is now idempotent (one-shot `roleInitialized` flag) so user-driven `switchRole()` survives composition cycles; new regression test in `DashboardViewModelTest`. **Messages tab**: removed the entire `FilterBar` (role / status / unread chips) from `ConversationsScreen` and the `currentRole` plumbing through `MessagesTabScreen` — iOS `ConversationsView` has no filter UI and `ChatAPIService.getConversations` takes no role param (backend returns every conversation; each row's `userRole` drives the per-row badge). `ChatScreensTest` updated. **System messages**: `PMessageBubble.System` (DS) now renders inside a centered `Surface` with `colorScheme.surfaceVariant` + `PasabayanRadius.md` (iOS `ConversationDetailView` `secondaryBackground` parity); `ChatMessageBubble` switched system text from `colorScheme.primary` to `onSurfaceVariant` — reads as status chrome, not a participant message. **DS-enforcement rule**: new `.cursor/rules/design-system-enforcement.mdc` codifies "every component is a `P*` primitive, every value is a `Pasabayan*` token" with a component/token catalog, cross-role parity clause, and the workflow for adding new primitives before consuming them; `CLAUDE.md` updated to reference. **Phone-verification gate (8 slices)** landed on top of Phase 6 — Android-only behavior delta vs iOS that enforces phone verification at four entry points (create package, create trip, book trip, request to carry). New: `AuthRepository.currentUser(): StateFlow<AuthUser?>` + `clearCurrentUser()`; `RequirePhoneVerificationUseCase`; `VerifyPhoneReason` enum + `VerifyPhonePromptSheet` shared composable; gate strings (en + fr). VMs gated and tested: `PackageViewModel` (`createPackageRequest` / `createServiceRequest` / `requestTripForPackage`), `TripCreationViewModel.createTrip`, `CreateTripFromPackageViewModel.createTrip`, `BrowseTripsViewModel.bookTrip` / `bookTripDirectly`, `MatchingViewModel.submitCounterOffer` (shipper + carrier branches) + new `requestPackageAsCarrier`. UI short-circuits in `MainTabScreen` + `ShipperExploreContent`; flag forwarding from `MatchListScreen`. Spec [10](10-verification.md) updated with gate map. See also: **Phase 5 notifications gate closed** (5 slices) and **Trips parity polish** (9 slices) landed earlier. **Notifications gate (slices 10–15)**: dashboard top-bar bell with unread badge (caps "99+"), `NotificationRouterEntryPoint` Hilt accessor lets composables resolve the singleton without prop-drilling, `MainActivity.handleNotificationDataExtra` (onCreate + onNewIntent, `singleTop` already set) parses `EXTRA_NOTIFICATION_DATA` via `PushNotificationRoute.fromPushData` and emits to `NotificationRouter`, `MainTabScreen` observer maps every `NavigationEvent` variant to tab switches / sub-screens, **`ActionableItemAggregator`** (pure function, 12 JVM tests) implements all 7 iOS items (booking requests, status updates, inactive trips, carrier responses, pickup ready, unread messages, verify/upgrade) plus the matching plurals (EN/FR), `MatchListScreen` gains `initialMatchId` + `initialCounterOfferMatchId` deep-link params with the corresponding `LaunchedEffect(_, state.matches)` to resolve once the list loads, **`CounterOfferPromptSheet` now wired** to both list/details `CounterOffer` actions + the deep-link, `PaymentsProfileScreen` gains `initialTransactionId` that routes straight into `TransactionDetailScreen`, and dashboard pre-warms `profileTabViewModel.loadTabData` so the verify-prompt reflects the real verification level on cold start. **Trips parity polish (slices 1–9)** on top of the prior Phase 2 base: `Trip.formattedPickupOrDepartureDate` / `formattedDeliveryOrArrivalDate` with `pickup_date ?? departure_date` fallback (plus "Not set" placeholder), capacity card now surfaces available space in liters, `TripMatchPackageCard` extracted with iOS-parity header (description + colored `MatchStatusBadge`) + `MatchRouteRow` + `MatchShipperRow` (name + ⭐ rating) + retained code state + chat pill + tap-anywhere-to-chat, `PackageSummaryJson` adds `pickup_city` / `delivery_city` / `fragile` / `package_type`, **`CarrierEarningsSection`** extracted with iOS-parity gating (renders only when total > 0), two-column rows, and locale-aware `formatTripCurrency` via `NumberFormat.getCurrencyInstance`, `TripDetailsScreen.kt` split from 1170 → 753 lines (helpers moved into `TripMatchPackageCard.kt` + `CarrierEarningsSection.kt`), `CarrierTripsViewModel.suspendUpdateTripStatus(...)` plus `TripStatusUpdateSheet` 15-second `withTimeoutOrNull` fallback with inline `isUpdating` spinner + dismiss-on-success, **`CreateTripFromPackageScreen`** now captures pickup/dropoff addresses, special notes, transport via `PFilterChip` row, and a single price input routed to `pricePerKg` (air/sea) or `flatTripPrice` (land); package info card renders weight + `package_type` row + Fragile/Urgent tags, raw ISO-string date inputs replaced with paired `TripDateTimePicker`s. **Prior context preserved**: Phase 4 (spec [06](06-payments-stripe.md)), Phase 6 (specs [09](09-profile-carrier-consent.md) / [10](10-verification.md) / [11](11-favorites-ratings.md) / [20](20-profile-tab.md)), and the spec-12 code surface for Phase 7 (legal sheet + support form + location catalog + GPS home-city detection + activity logger) landed in earlier sessions. |
| **Spec audit** | **Complete** — YAML expanded from ~25 to ~100 endpoint rows; all feature specs updated with query params, multipart fields, WebSocket protocol, local storage keys, activity logs, cache policy, GPS services, badge rules, analytics mock structures |

---

## Phases (exit gates)

| Phase | Exit gate | Status |
|-------|-----------|--------|
| **0** — Foundation | See [PHASES-AND-FEATURES.md](PHASES-AND-FEATURES.md) § Phase 0 | **Complete** — foundation modules, API-SHAPES (auth), **ErrorAlertPolicy**, TDD Phase 0 backlog ticked; app shell polish remains in later phases |
| **1** — Authentication | Login, token, `/auth/me`, logout | **Complete** — OAuth UI (Google/Facebook → backend); cold-start onboarding → auth; post-login city → consent → dashboard shell; **`UnauthorizedSessionNotifier`** + **`AuthViewModel`** signed-out on **401**; `didJustCompleteConsent` one-shot; auth DTO tests + **`AuthRepositoryIntegrationTest`** + **`TokenClearingHandlerTest`**. Optional: Credential Manager / One Tap, extra `AuthViewModel` tests. |
| **2** — Trips, packages, Explore | Browse/list/create flows + role tabs | **Complete** — `:core:domain` (15 enums, 4 shared models, FlexibleDecoders, DateTimeParsing); 12 new `P*` design-system components; tab shell with role switcher. **Trips** parity slices landed across API/repository/VM/UI/store/TDD: endpoints in `TripsApi` (`/routes/popular-packages`, `/route-activity/summary`, `/trips/{id}/matches`, `/packages/{id}/trip-template`), create-trip special error taxonomy, `PopularRoute` + `RouteActivitySummary` contract alignment, booking/compatibility state in `BrowseTripsViewModel`, explicit `cancelTrip()` blocking in `CarrierTripsViewModel`, `TripPackageProgressViewModel` idle + status labels, new UI surfaces (`TripFilterSheet`, `EditTripSheet`, `CreateTripFromPackageScreen`), carrier `TripDetailsScreen` sections, and local-state coordinator wiring (`UsualTransportStore`, saved-route autosave, disclaimer pending-sync retry hook). Decode/repository/VM/store tests were expanded, including `TripUpdateRequestJson` planning/non-planning encode constraints. **Packages**: API/DTO/domain/repository/VM/UI + `PackageFormValidator` tests remain complete. **Explore**: `CarrierExploreContent` + `ShipperExploreContent` surface route activity/popular routes; EN/FR strings updated. |
| **3** — Bookings & matches | Core marketplace loop + counter-offer parity | **Complete** — BookingsApi (20+ endpoints), DeliveryMatch (60+ fields) + BookingAction (10 cases) + BookingMapper + computed props (availableActions, pricing, codes); BookingType enum; 9 nested info types; 10 supporting models (CounterOfferContext, codes, tracking, stats); BookingsRepository (15 methods); MatchingViewModel + LiveTrackingViewModel + AutoChargeConfirmationViewModel; unified MatchCard (replaces 4 iOS cards) + MatchStatusBadge + PriceComparison + CounterOfferBanner + code views; RequestToCarrySheet + CounterOfferPromptSheet + AutoChargeSheet + RateDeliverySheet; unified MatchListScreen wired to tab; BookingSuccessScreen + code screens; 60+ tests; full i18n (EN+FR). |
| **4** — Payments & Stripe | PaymentSheet, transactions, Stripe Connect onboarding + dashboard | **Complete** — PaymentSheet parity ✅, transactions parity ✅, **Stripe Connect parity ✅**: debounce + single-flight `loadStatus` (2 s window, Mutex), typed `StripeConnectError` (NotCarrier / AlreadyOnboarded / NotOnboarded / ApiError) mapped from server messages, dedicated `PayoutSetupScreen` with all 4 iOS-parity states (loading / not setup / partial / complete) plus security notice + status checkmarks, Chrome Custom Tabs (`androidx.browser`) onboarding + dashboard with `handleOnboardingReturn` forced-refresh, profile menu deep-link, EN/FR strings. New tests: VM debounce / forceRefresh / `shouldStartOnboarding` guard, repository business-message mapping. |
| **5** — Chat & notifications | Realtime chat + FCM device-token routing + deep-link parity | **Complete** — `07-chat-broadcasting` hardening landed in earlier sessions (realtime protocol/parser robustness, reconnect/polling state handling, thread + conversations parity, expanded tests). **`08-notifications-device-tokens` end-to-end**: NotificationApi + DTOs + 4-shape `UnreadCountResponseParser`, `NotificationType` (21 values), `NotificationRepository` with 403 push_notifications consent auto-retry, `NotificationLifecycleManager` login/logout hooks, `PasabayanFirebaseMessagingService` foreground display + `PendingIntent`, `NotificationRouter` SharedFlow + `NotificationRouterEntryPoint` Hilt accessor, `NotificationViewModel` with per-role unread counts + optimistic mark-as-read, `ComprehensiveNotificationsScreen` (3 sections) + components + EN/FR strings. **Gate-closing wiring**: dashboard top-bar bell with unread badge ("99+" cap) opens the sheet, `MainActivity.handleNotificationDataExtra` parses `EXTRA_NOTIFICATION_DATA` on cold start + `onNewIntent` and emits via `NotificationRouter.emitFromPush`, `MainTabScreen` observer routes every `NavigationEvent` variant (per-id deep links for `OpenMatch` → `MatchListScreen.initialMatchId`, `OpenCounterOffer` → `MatchListScreen.initialCounterOfferMatchId` opening the now-wired `CounterOfferPromptSheet`, `OpenTransactionDetail` → `PaymentsProfileScreen.initialTransactionId` → `TransactionDetailScreen`, `OpenConversation` → existing `pendingConversationId`), `ActionableItemAggregator` builds all 7 iOS items (booking requests / status updates / inactive trips / carrier responses / pickup ready / unread messages / verify-or-upgrade) from `MatchingViewModel.matches` + `CarrierTripsViewModel.trips` + `PackageViewModel.packageRequests` + `ConversationsViewModel.allUnreadCount` + `ProfileTabViewModel.userProfile.verificationLevel` (pre-warmed on dashboard mount). EN/FR plurals for each counted spec. 12 new JVM tests on the aggregator. |
| **6** — Profile, verification, favorites & ratings | Profile editing + disclaimers/consent + verification + favorites + ratings | **Complete** — [09](09-profile-carrier-consent.md) full surface: user profile edit (EditUserProfileSheet with smart pre-fill + additional_info merge), avatar upload + delete with 512px JPEG compression, carrier profile edit + preferences with create/update branching + enableCarrier, privacy preferences (scoped consent updates + disable-confirm), disclaimer bootstrap + retry-pending-sync, account deletion + GDPR data export, Settings screen with currency picker + clear-cache. [10](10-verification.md) phone OTP (E.164 normalisation + 60s resend cooldown) + premium ID/selfie multipart with status tracking. [11](11-favorites-ratings.md) favorites list with sort filter + remove + send-request sheet + scoped error mapping (409 → AlreadyFavorited, 400 → CannotFavorite(message)), tabbed Feedback screen for received/given/pending with in-place comment editing. [20](20-profile-tab.md) all menu deep-links wired (Personal info / Vehicle info / Verification / Payments hub / Settings / Favorites / Pending reviews / Account & data). 80+ new JVM tests. |
| **7** — Legal, support, misc | Legal agreements, support ticket, location catalog, GPS home-city detection, activity logs | **Spec [12](12-legal-support-misc.md) Complete** — code surface from earlier sessions (legal sheet, support form, location catalog, GPS detection, activity logger) plus this session's bootstrap closeout: `SessionAuthSyncObserver` (singleton, started from `PasabayanApplication.onCreate` on an app-scope `SupervisorJob() + Dispatchers.Default`) observes `AuthRepository.currentUser()` and on each emission (a) updates `SessionActivityLogUserProvider.update/clear` so `ActivityLogger` carries a real user_id, and (b) triggers `HomeCityDetectionService.detectIfNeeded()` (the service's own `hasRunThisSession` guard makes re-emissions a no-op). Settings → Terms & Privacy menu row routes to `LegalViewerSheet` (was a placeholder). Profile-menu deep-links into Help Center / Settings / Terms / Support form already verified. **Remaining Phase 7 follow-up**: RouteActivity dropdown (CarrierExploreDropdownState port + Recent/Popular search dropdown UI). |

---

## Phase 0 — What is implemented (codebase)

| Item | Status | Notes |
|------|--------|--------|
| Gradle **Version Catalog** (`gradle/libs.versions.toml`) | Complete | AGP 8.8.2, Gradle 8.10.2, Kotlin 2.2.10, KSP, Compose |
| **`:core:designsystem`** | Complete | Core primitives: `PasabayanTheme`, tokens (`PasabayanBorder`, `PasabayanLayout`, `PasabayanMotion`, `PasabayanTextStyles`), `ds*` modifiers, `PButton`, `PCard`, `POutlinedTextField`, `PScaffold`, `PSnackbar`/`PSnackbarHost`, `PModalBottomSheet`, `PTopBar`, `PDivider`, `PCircularProgress`; JVM + androidTest. Spec **14** full color inventory / domain composites (e.g. `EmptyStateView`) still optional as features land. |
| **`:core:domain-error`** | Complete | `DomainError` + `ValidationError`; [01-error-taxonomy.md](01-error-taxonomy.md); **`userMessage()`**; **`ErrorAlertPolicy`** / `ErrorAlertContext` (foreground vs background, iOS parity) + JVM tests |
| **`:core:network`** | Complete | OkHttp + Retrofit + kotlinx-serialization; Hilt `NetworkModule`; `AuthInterceptor` (Bearer except `/auth/*` without `/auth/me`); **`ApiErrorMapper`** + error DTOs; **`Response.toDomainResult` / `foldDomainResult`**, **`ResponseExt` helpers**; `ProfileApi` + profile/carrier DTOs + fixtures; `BuildConfig.API_BASE_URL` = production; `consumer-rules.pro` |
| **`:app`** | Partial | `@HiltAndroidApp` `PasabayanApplication`; `@AndroidEntryPoint` `MainActivity`; main shell; **Profile** feature (`ProfileRepository`, `ProfileTabViewModel`, `ProfileTabScreen`) wired in `MainTabScreen` (tab + payments hub) |
| **Hilt** | Complete | App + `:core:network` (KSP) |
| **Google Services / Firebase** | Complete | `com.google.gms.google-services` plugin; `app/google-services.json`; Firebase BOM + **firebase-messaging**; **`PasabayanFirebaseMessagingService`** wired (token registration via `NotificationLifecycleManager`, foreground heads-up via `NotificationDisplay` + `NotificationChannelSetup`); **push-tap deep-link routing** wired via `MainActivity.handleNotificationDataExtra` → `NotificationRouter` → `MainTabScreen` observer (per-id deep links across matches / counter-offer / transactions / conversations). |
| **Google Sign-In** | Partial | `GoogleSignInHelper` + `AuthScreen` / `AuthViewModel`; Credential Manager / One Tap **not** wired (optional) |
| **Facebook Login** | Partial | `FacebookLoginStarter` + `LoginManager`; `MainActivity` forwards `onActivityResult` to `CallbackManager` |
| **Manifest & security** | Complete | `INTERNET`, `ACCESS_NETWORK_STATE`, `POST_NOTIFICATIONS`, `WAKE_LOCK`; `network_security_config` (HTTPS; debug cleartext for localhost); backup / data-extraction XML |
| **Tests** | Complete | `AuthInterceptorTest` + **`ApiErrorMapperTest`**; **`ErrorAlertPolicyTest`** (`:core:domain-error`); **`AuthRepositoryIntegrationTest`** (MockWebServer + Retrofit, login / me / logout) in `:core:session`; auth JSON decode tests in `:core:network`; design system JVM + androidTest |

---

## Phase 1 — What is implemented (codebase)

| Item | Status | Notes |
|------|--------|--------|
| **`:core:session`** | Complete | `TokenStore`, `EncryptedTokenStore`; `StoredAuthTokenProvider`; `TokenClearingHandler` → `SessionInvalidationHandler` + **`UnauthorizedSessionNotifier`** (401 → UI); `AuthRepository` / `AuthRepositoryImpl`; `SessionModule` (Hilt); **`TokenClearingHandlerTest`** |
| **`app` dependency** | Complete | `implementation(project(":core:session"))` |
| **OAuth UI** | Complete | `AuthScreen` + `AuthRoute` + `AuthViewModel`; Google + Facebook → backend; **`DashboardScreen`** placeholder; **401** + `error_unauthorized`; **`didJustCompleteConsent`** via dashboard |
| **Onboarding (17)** | Complete | `RootViewModel` / `AppEntryContent`; `OnboardingRoute`; city + consent gates; prefs keys; consent failure still advances; **city + consent profile writes** go through `ProfileApi` (aligned with [09](09-profile-carrier-consent.md) HTTP contract) |

---

## Phase 6 — What is implemented (complete)

| Item | Status | Notes |
|------|--------|-------|
| [20-profile-tab.md](20-profile-tab.md) | **Complete** | Profile tab shell with section order + role gating; **all menu deep-links wired**: Personal info → `EditUserProfileSheet`, Vehicle info (carrier only) → `EditCarrierProfileSheet`, Verification → `PhoneVerificationSheet` (with premium upgrade entry), Payments hub sub-route, Settings → `SettingsScreen` sub-route, Favorites → `FavoritesListScreen` sub-route, Pending reviews → `RatingsScreen` sub-route, Account & data → `AccountManagementSheet`. Compose previews + EN/FR strings + JVM + androidTest. TDD checklist green: section visibility, verification card visibility, avatar cache-buster, **load dedupe + forceRefresh**, navigation routes wired in `MainTabScreen`; logout-E2E and localization-lint rows remain documented as deferred per spec. |
| [09-profile-carrier-consent.md](09-profile-carrier-consent.md) | **Complete** | Repository surfaces all 16 endpoints. **Edit user profile** with smart pre-fill (profile → social-auth → email/phone-verified contact) + `additional_info` merge preserving legacy `oauth_*` / `language` keys. **Avatar** Photo Picker → `BitmapImageCompressor` (512 px JPEG q70) → `POST /profile` multipart preserving form fields; delete with destructive confirm dialog. **Carrier profile** create/update branching with 409 → GET fallback, `enableCarrier` after first create, `availableRoutes` + `preferredPickupCityId` preservation, package-type + restricted-item multi-selects. **Privacy preferences** sheet with scoped single-key `PUT`, optimistic + revert-on-failure, disable-confirm for push + location. **Disclaimer sync service** (bootstrap from server + retry-pending-syncs) wired via `MainTabScreen` `LaunchedEffect(user)` alongside legacy carrier-side retry. **Account deletion** with reason input, HTTP 202 success, sign-out on success. **GDPR data export** → pretty-printed JSON written to `cacheDir` → FileProvider share intent. **Settings screen** with role display + currency picker (`PreferredCurrencyStore`) + clear-cache + sub-flow routing into carrier-preferences / privacy / account-data. |
| [10-verification.md](10-verification.md) | **Complete** | **Phone OTP**: 4 endpoints, E.164 normalisation (`(514) 555-1234` → `+15145551234`), 60s resend cooldown, bootstrap resumes OTP step when `pendingVerifications > 0`. **Premium ID**: 3 endpoints including multipart `POST /verification/request-premium`, `IdDocumentType` enum (5 values; passport single-sided, others front+back), `ImageCompressor` reuse for ID images + selfie, status surfacing for existing pending applications. Premium entry point sits on the phone-verification verified card. |
| [11-favorites-ratings.md](11-favorites-ratings.md) | **Complete** | **Favorites**: 6 endpoints, list with sort chips (recent / most_used / rating) + "upcoming trips only" filter, remove with pending-removal tracking, send-request sheet with 4 cards (pickup / delivery / package / message); custom error mapping (409 → `AlreadyFavorited`, 400 → `CannotFavorite(message)`, 404 → `NotFound`). **Ratings**: 4 endpoints, tabbed Feedback screen with Received summary card, Given list with inline `PUT /ratings/{id}/comment` editing, Pending list with route + days-since-delivery (tolerant `FlexibleDoubleSerializer` decode). |

**Phase 6 exit gate (from [PHASES-AND-FEATURES.md](PHASES-AND-FEATURES.md)):** *Profile editing, disclaimers/consent, verification flows, favorites and ratings* — **met**.

**Deferred to follow-up specs** (not blockers for Phase 6 gate): home-city autocomplete + 10-minute cooldown (settings polish), Coil/Glide image rendering for avatars + carrier cards (cross-cutting), addFavorite UI entry point (lives in match/booking flows from spec 05), sent-direct-requests list UI, pagination on ratings lists.

---

## Phase 0 — Spec / artifact checklist

| Spec / artifact | Done | Notes |
|-----------------|------|-------|
| [16-project-bootstrap.md](16-project-bootstrap.md) | [x] | Modules, Hilt, Retrofit, theme, `assembleDebug` |
| [00-architecture.md](00-architecture.md) | [x] | Core module split; lean stack documented in Gradle |
| [01-error-taxonomy.md](01-error-taxonomy.md) | [x] | **HTTP status → `DomainError`** via `ApiErrorMapper`; **`userMessage()`**; **`ErrorAlertPolicy`** + tests |
| [14-design-system.md](14-design-system.md) | [x] | Core `P*` set + `ds*` + tokens + JVM/androidTest; DS-0–DS-7 done in TDD table. Large **14** inventory (every semantic color row, domain composites) grows with features. |
| [API-SHAPES-REFERENCE.md](API-SHAPES-REFERENCE.md) | [x] | **Implemented auth DTOs** audited: golden JSON fixtures + `AuthJsonModelsDecodeTest` in `:core:network` (`src/test/resources/api-fixtures/auth/`). Re-audit when new Retrofit/DTO surfaces land (trips, packages, …). |
| [contracts/api-contract-matrix.yaml](contracts/api-contract-matrix.yaml) | [x] | **Expanded** from ~25 to ~100 endpoints; 6 new groups (favorites, ratings, verification, stripe_connect, stripe_payment_methods, support) + expanded all existing groups |
| [IMPLEMENTATION-GUIDE.md](IMPLEMENTATION-GUIDE.md) | — | Process doc; no “done” checkbox |
| [TDD-PARITY-BACKLOG.md](TDD-PARITY-BACKLOG.md) | [x] | Phase 0 rows ticked (interceptor, mapper + ErrorAlertPolicy, design system); expand per backlog when features land |

**Phase 0 exit gate (from [PHASES-AND-FEATURES.md](PHASES-AND-FEATURES.md)):** **Error mapping + user-facing strings** are in place; **contract YAML audit complete** (expanded to ~100 endpoints); **[API-SHAPES-REFERENCE.md](API-SHAPES-REFERENCE.md) verified for implemented Android DTOs** (auth — golden JSON tests in `:core:network`); extend fixtures as new DTO modules ship. **ErrorAlertPolicy** (foreground vs background) implemented in `:core:domain-error`. [TDD-PARITY-BACKLOG.md](TDD-PARITY-BACKLOG.md) Phase 0 complete.

**Phase 1 exit gate:** Login → token persist → **`/auth/me`** → logout; auth DTO tests; onboarding order per [17-onboarding.md](17-onboarding.md). **Met** for shipped scope (see phase table).

---

## Feature specs (remaining phases)

| Spec | Done |
|------|------|
| [02-auth-session.md](02-auth-session.md) | [x] | App + `:core:session` meets exit gate; optional extra VM/repo tests later |
| [17-onboarding.md](17-onboarding.md) | [x] | Flows + keys + gates; carrier consent flash uses `didJustCompleteConsent` when carrier UI ships |
| [03-trips.md](03-trips.md) | [x] | Complete for current parity scope — trip creation depth/integration blockers were closed: wizard/review flow, reusable `CityAutocompleteField` + local city autocomplete UX, in-flow `SavedRoutesSheet` apply, keyboard focus chain, success + save-route prompt, and `TripTutorialOverlay` primary-journey wiring with test coverage. |
| [04-packages.md](04-packages.md) | [x] | Strict parity bridge landed: package detail/edit/cancel flow wired through dashboard sheet routes, carrier explore moved to available-packages data path, package creation journeys integrate disclaimer + tutorial + saved descriptions/routes stores, packages VM/UI copy localized (EN/FR), and package stores/view-model coverage expanded with new tests. |
| [13-ui-tab-explore.md](13-ui-tab-explore.md) | [x] | Tab shell, role switcher, carrier + shipper explore content, stats grids; Matches/Messages; Profile tab per [20-profile-tab.md](20-profile-tab.md) (shell + payments hub) |
| [05-bookings-matches.md](05-bookings-matches.md) | [x] | Unified MatchCard + MatchListScreen, 3 ViewModels, counter-offer flows, code screens, 60+ tests |
| [06-payments-stripe.md](06-payments-stripe.md) | [x] | **iOS-parity sweep landed** (29 slices across A–E, see [PAYMENTS-IMPLEMENTATION-PLAN.md](PAYMENTS-IMPLEMENTATION-PLAN.md)). **Domain widening** (A1–A6): `Transaction` now composes `TransactionUser / TransactionAmounts / StripeInfo / RefundInfo / TransactionTimestamps / Payout / TipInfo` (was 15 flat fields); `PaymentReceipt` nested with role-aware `displayAmount`; new `RefundRequest` domain + `MoneyFormatter` (locale-aware) + Composable `refundReasonDisplayText` helper; mapper preserves `metadata` JSON + nested timestamps with top-level fallback. **VM depth** (B1–B5): `PaymentMethodsViewModel` add-card lifecycle (`sealed AddCardFlowState`); `PaymentViewModel` `paymentSheetConfig` + `handle3DSChallenge` + `pollForConfirmation`; `TippingViewModel` `PaymentSheetResultKind` bridge; `RefundViewModel` swapped to domain + silent `checkRefundStatus` polling; `TransactionDetailViewModel` role-aware computeds (`canRequestRefund`, `canAddTip`, `showEarningsCard`/`AmountBreakdown`, `showRefundStatusCard`, `showPayoutStatusCard`). **8 reusable components** (C1–C7) in `features/payments/components/`: `PayoutStatusBadge`, `RefundStatusBadge`, `TipBadge`, `PaymentMethodCard`, `PaymentSummaryCard` + `PaymentSummaryComputation`, `PaymentButton`, `RefundStatusCard`, `ReceiptRow` — token-based colors (added `PasabayanColors.BadgePink`/`BadgePinkLight`). **7 spec'd screens** (D1–D7): `PaymentMethodsScreen` + self-hosted `AddPaymentMethodSheet`, `RefundSheet` (validated), rebuilt `TransactionDetailScreen` (role-aware sections), `TipSelectionScreen`, paginated `ReceiptListScreen`, `ReceiptDetailScreen` + `ReceiptWebView`. **Wiring** (E1–E4): profile sections refactored to compose `PaymentMethodCard` + `TipBadge`; 3 new top-level routes in `MainTabScreen` (methods / receipts / receipt detail); `pasabayan://stripe-redirect` manifest filter + defensive `MainActivity.handleStripeRedirect`; private `rememberDebouncedClick` on hub-entry callbacks. **Tests/strings**: ~95 new EN/FR string keys, ~50 JVM tests across model/components/VM, 4 androidTest Compose suites (`PaymentMethodCardTest`, `PaymentMethodsScreenTest`, `TipSelectionScreenTest`, `RefundSheetTest`), ~25 light+dark previews. **Deferred to follow-up**: `TransactionDetailScreen.onRequestRefund`/`onAddTip` still auto-submit (need to launch `RefundSheet`/`TipSelectionScreen`); debug-input `TippingRefundSection` + `PaymentProcessingSection` slated for removal once real flows are reachable. |
| [07-chat-broadcasting.md](07-chat-broadcasting.md) | [x] | Core + hardening + parity closeout landed in app + `:core:network` + `:core:designsystem`: contracts/decode tests, repository/realtime/merge logic, full-flow realtime protocol tests (including nested subscription-success payload), parser hardening for wrapped/direct string/object payloads, reconnect auto-resubscribe verification, ViewModel decode-recovery + polling coalescing + delete/retry/no-duplicate-temp coverage, thread/conversation UI parity upgrades, DS chat primitives adoption, and EN/FR chat localization resources. Phase 5 gate **closed** alongside spec [08](08-notifications-device-tokens.md). |
| [08-notifications-device-tokens.md](08-notifications-device-tokens.md) | [x] | End-to-end: DTOs + repository + FCM lifecycle + VM + router + UI + dashboard bell with unread badge + `MainActivity` push-tap parsing + per-id deep links (`OpenMatch` / `OpenCounterOffer` / `OpenTransactionDetail` / `OpenConversation`) + `ActionableItemAggregator` covering all 7 iOS items. Test suite expanded with `ActionableItemAggregatorTest` (12 cases). |
| [09-profile-carrier-consent.md](09-profile-carrier-consent.md) | [x] | Edit user/carrier profile, avatar upload/delete, consent prefs (scoped + revert), disclaimer sync, account deletion, GDPR export, Settings screen — see Phase 6 detail above. |
| [10-verification.md](10-verification.md) | [x] | Phone OTP flow + premium ID/selfie multipart — see Phase 6 detail above. |
| [11-favorites-ratings.md](11-favorites-ratings.md) | [x] | Favorites list + send-request + tabbed Feedback (received/given/pending) — see Phase 6 detail above. |
| [12-legal-support-misc.md](12-legal-support-misc.md) | [x] | Legal API+repo+VM+sheet, support multipart+form, location catalog+nearby carriers, home-city GPS detection, activity logger + health, 98 JVM tests — **plus** Settings→Terms wired to `LegalViewerSheet`, and `SessionAuthSyncObserver` bootstrap (`PasabayanApplication.onCreate` → `AuthRepository.currentUser()` → `SessionActivityLogUserProvider.update/clear` + `HomeCityDetectionService.detectIfNeeded()`). |
| [15-platform-and-tab-index.md](15-platform-and-tab-index.md) | [ ] | Pending — full parity audit not completed yet. |
| [20-profile-tab.md](20-profile-tab.md) | [x] | Tab shell + all menu deep-links wired (Personal info / Vehicle info / Verification / Payments hub / Settings / Favorites / Pending reviews / Account & data); TDD checklist green except deferred logout-E2E and localization-lint rows. |

---

## 03-trips parity notes (recently resolved)

- **Trip creation UX depth (`03-trips.md` Trip creation UX detail)**  
  `TripCreationScreen` now supports wizard/full-review transitions, local city autocomplete catalog UX, in-flow saved-route apply, keyboard focus chain behavior, and success flow with save-route prompt.

- **Trips UI integration coverage (`03-trips.md` screens/components integration)**  
  `EditTripSheet`, `CreateTripFromPackageScreen`, `TripFilterSheet`, carrier `TripDetailsScreen`, and primary creation-journey integrations are now wired with deterministic route/open-close behavior coverage.

- **Local/client integration depth (`03-trips.md` Local state section)**  
  `SavedRoutesSheet` and `TripTutorialOverlay` are now integrated in primary trip creation journeys; `UsualTransportStore` and saved-route persistence are applied in the manual creation success path.

- **TDD checklist parity (`03-trips.md` TDD checklist)**  
  Coverage now includes trip creation flow-state transitions, saved-route store behavior, trip-creation view-model state machine, success-flow save/skip branch assertions, and route determinism tests alongside prior API/repository/VM/store closeout coverage.

- **Trip details iOS-parity polish (2026-05-11 session)**  
  Schedule card uses `pickupDate ?? departureDate` / `deliveryDate ?? arrivalDate` with "Not set" placeholder (iOS `scheduleInformationCard`); capacity card now surfaces available space in liters; `TripMatchPackageCard` extracted with iOS-parity header (description + colored `MatchStatusBadge`) + pickup→delivery city row + shipper name + ⭐ rating + retained code state + chat pill + tap-anywhere-to-chat; `PackageSummaryJson` adds `pickup_city` / `delivery_city` / `fragile` / `package_type`; `CarrierEarningsSection` gates on total > 0 with two-column rows + locale-aware `formatTripCurrency`; `TripDetailsScreen.kt` split 1170 → 753 lines.

- **TripStatusUpdateSheet 15s timeout fallback**  
  Port of iOS `TripUpdateTimeoutCoordinator` via Kotlin `withTimeoutOrNull` + `rememberCoroutineScope`. `CarrierTripsViewModel.suspendUpdateTripStatus(...)` returns `Result<Trip>`; sheet drives inline `isUpdating` spinner + dismiss-on-success + timeout/error inline messages. 4 new JVM tests covering success / invalid transition / unknown trip / repo failure.

- **`CreateTripFromPackageScreen` iOS-parity (`03-trips.md` create-from-package)**  
  Package info card with weight + `package_type` row + Fragile / Urgent tags; transport selector replaces raw text input with `PFilterChip` row over `FLIGHT/CAR/BUS/TRAIN/SHIP`; pickup / dropoff address + special notes fields restored (previously dropped on submit); single price input routes to `pricePerKg` (air/sea) or `flatTripPrice` (land) based on transport; raw ISO date strings replaced with paired `TripDateTimePicker` (epoch-millis state + ISO at submit). `CreateTripFromPackageRequestBuilderTest` (10 cases) + mapper / decode tests for the new `package_type` field.

- **Trips parity sweep — A through G (2026-05-13 session)** — every gap in [TRIPS-PARITY-REVIEW.md](TRIPS-PARITY-REVIEW.md) closed.
  - **A — activate endpoint**: `POST /trips/{id}/activate` added to `TripsApi`, surfaced via `TripsRepository.activateTrip` and `CarrierTripsViewModel.activateTrip` / `suspendActivateTrip`. `CarrierMyTripsScreen` Activate dialog now routes here instead of the silently-dropped `PUT /trips/{id}` with `trip_status=active`. Repo test asserts path + method + empty body; VM tests pin success / non-planning rejection / repo-failure paths.
  - **B1–B6 — EditTripSheet expansion (161 → ~750 LOC, iOS' 1,593 LOC parity)**: route, schedule, capacity, pricing branches each extracted into a reusable `components/EditTrip*Section.kt`. `TripFormState` carries country + shared-pickup/delivery millis; `TripFormValidator` enforces `MAX_WEIGHT_KG=2000` / `MAX_SPACE_LITERS=5000` / `MAX_PRICE_PER_KG=100` (new `WeightOutOfRange` / `SpaceOutOfRange` / `PricePerKgOutOfRange` errors). `onSave` switched from `(weight, notes)` to a partial `TripUpdateRequestJson` that emits only changed fields and drops route/schedule when the trip has left planning (iOS `canEditRoute`). In-sheet Activate confirmation + destructive Cancel Trip with HTTP 409 → new `DomainError.TripHasBlockingMatch` mapping that joins server message with `trips_details_cancel_error_match_in_progress_hint`. `DomainErrorMessages.userMessage()` + `ErrorAlertPolicy` exhaustive-when branches added.
  - **C — request-to-carry alert + `clearCreatedTrip` semantics**: `CreateTripFromPackageViewModel` adds `requestToCarryMessage: String?` and detects iOS' error phrases (`request to carry` / `compatible trip` / `has compatible trips` / `already have`+`trip`, case-insensitive). Screen renders the alert with primary "Request to carry" → fires new `onRequestToCarry(packageId)` callback. `clearCreatedTrip` doc-commented as a one-shot consumer (not a form reset).
  - **D — passenger-mode UI verified closed**: exhaustive iOS search confirmed `passengerCapacity` / `pricePerPassenger` / `passengerRequirements` / `ageRestrictions` / `passengerAmenities` are dormant `Trip.swift` decode-only fields with no UI binding and no presence in `CreateTripRequest` / `TripUpdateRequest`. §5 closed with evidence inline in the parity review.
  - **E — `TripStatusUpdateSheet` aligned with iOS matrix**: `nextStatusOptions` now opens `CANCELLED` from `PLANNING` + `ACTIVE` (iOS `statusPickerOptions:228`); the sheet exposes `onActivate` + `onCancel` callbacks, dispatches by target with iOS-parity confirmation alerts (activate confirm + destructive cancel/keep-trip), and reuses Slice B6 blocking-match formatting. `TripDetailsScreen` and `CarrierMyTripsScreen` plumb both new callbacks through to `suspendActivateTrip` / `suspendCancelTrip`.
  - **F — pricing-breakdown visualization**: new reusable `components/TripPricingBreakdown` composable renders `basePrice` + `distance` + `distance multiplier` + `calculatedPrice` as a tinted block under the headline price in `TripDetailsScreen` when the server returns the breakdown. `CarrierEarningsSection` audited — already iOS-parity, no change.
  - **G — finish `populateFromTemplate`**: `TripTemplateJson` gains `suggested_transportation_method`; `CreateTripFromPackageRequest` carries `sharedPickupDate` / `sharedDeliveryDate`; `TripsRepositoryImpl.createTripFromPackage` forwards both onto wire `pickup_date` / `delivery_date`; `CreateTripFromPackageScreen` auto-preselects transport via new `internal fun parseTransportationMethod(raw: String): TransportationMethod?` and auto-derives shared pickup/delivery from departure/arrival in `buildRequestFromState` — closes iOS `populateFormFromTemplate:202-217` + `syncCarrierLegFromSharedSchedule`.
  - **Tests**: `TripsRepositoryImplTest` (activate happy/4xx/network; cancel 409 mapping; createTripFromPackage shared-schedule on/off), `EditTripSheetRequestTest` (route + schedule + capacity + pricing emission, locked drops, unchanged suppression, snake_case wire), `TripFormValidatorTest` (2000 kg / 5000 L / $100 boundaries + land flat ≥100 doesn't trip per-kg cap), `CarrierTripsViewModelTest` (activate + suspendActivate + suspendCancel + editTrip), `CreateTripFromPackageViewModelTest` (5 request-to-carry phrase / case-insensitive / unrelated / consume + transport preservation), `CreateTripFromPackageRequestBuilderTest` (shared-schedule defaults + parser happy/blank/unknown), `TripJsonModelsDecodeTest` (suggested_transportation_method decode + null), `TripDetailsSectionsModelTest` (transition matrix flipped — planning → active+cancelled, active → in_transit+cancelled, in_transit forward-only).
  - **Strings** (EN + FR): added across sweep — `trips_edit_shared_pickup` / `_delivery`, `trips_edit_capacity_section`, `trips_edit_pricing_section`, `trips_edit_status_section`, `trips_edit_status_locked_note`, `trips_create_validation_weight_out_of_range`, `trips_create_validation_space_out_of_range`, `trips_create_validation_price_per_kg_out_of_range`, `trips_details_cancel_error_match_in_progress_hint`, `trips_edit_cancel_error_generic`, `trips_from_package_alert_trip_exists_title`, `trips_from_package_alert_request_to_carry`, `trips_status_update_hint_cancelled`, `trips_detail_pricing_{base,distance,multiplier,calculated}_label`, `trips_detail_distance_km_format`, `trips_detail_multiplier_format`. Existing `trips_action_activate_trip*` / `trips_cancel_trip` / `trips_action_cancel_trip_message` / `trips_action_keep_trip` reused.

---

## iOS feature folders → Android (from [FEATURE-COVERAGE-MATRIX.md](FEATURE-COVERAGE-MATRIX.md))

- [x] `Authentication/` — Complete — `:core:session` + **AuthScreen** / **AuthRoute** / **AuthViewModel**; **401 → signed-out**  
- [ ] `Analytics/` — Pending — [19-analytics.md](19-analytics.md) not started.  
- [x] `Bookings/` — Complete — Phase 3 scope landed.  
- [x] `Chat/` — Complete — contracts + realtime service + repository/merge + ViewModels + UI/messages tab + localization landed; chat-pill → Messages tab + per-id `OpenConversation` deep link wired via dashboard observer.  
- [x] `Favorites/` — Complete — list/sort/filter/remove + send-request sheet wired from profile menu; custom error mapping for 409/400/404.  
- [~] `Legal/` — Code surface complete — `LegalApi` (status/agree/withdraw) + repo + `LegalAgreementViewModel` (optimistic agree, partial outcomes) + `LegalAgreementSheet` (linear progress, doc tabs, bundled HTML articles via `AssetArticleWebView` with EN/FR fallback) + EN/FR strings; remaining: profile/help menu deep-link.  
- [x] `Notifications/` — Complete — FCM token register/unregister wired into login/logout, foreground heads-up display, `NotificationRouter` SharedFlow + `NotificationRouterEntryPoint` Hilt accessor + per-type routing table, dashboard top-bar bell + unread badge, `MainActivity` push-tap parses `EXTRA_NOTIFICATION_DATA` on cold start + `onNewIntent`, per-id deep links land on the specific match / counter-offer / transaction / conversation, `ActionableItemAggregator` aggregates 7 iOS items across bookings / packages / trips / chat / verification, `ComprehensiveNotificationsScreen` (3 sections).  
- [x] `Onboarding/` — Complete — Phase 1 scope landed.  
- [x] `Packages/` — Complete — strict `04-packages.md` parity bridge landed (detail/edit/cancel flow wiring, carrier available-packages path split, local store journey integration, localization hardening, and test/spec sync).  
- [x] `Payments/` — Complete — iOS-parity sweep landed (29 slices, A–E in [PAYMENTS-IMPLEMENTATION-PLAN.md](PAYMENTS-IMPLEMENTATION-PLAN.md)): nested `Transaction`/`PaymentReceipt`/`RefundRequest` domain; 5 widened VMs (add-card lifecycle, 3DS rotation, confirm-capture polling, PaymentSheet result bridge, silent refund polling, role-aware computeds); 8 reusable `P*`-style components (`PaymentMethodCard`, `PaymentSummaryCard`, `PaymentButton`, `PayoutStatusBadge`, `RefundStatusBadge`, `RefundStatusCard`, `TipBadge`, `ReceiptRow`); 7 spec'd screens (`PaymentMethodsScreen` + `AddPaymentMethodSheet`, `RefundSheet`, rebuilt `TransactionDetailScreen`, `TipSelectionScreen`, `ReceiptListScreen`, `ReceiptDetailScreen` + `ReceiptWebView`); profile-hub sections refactored to compose the new primitives; 3 new top-level routes in `MainTabScreen`; `pasabayan://stripe-redirect` manifest filter + defensive `MainActivity` handler; ~50 JVM tests + 4 androidTest Compose suites; ~95 EN/FR strings. Phase 4 exit gate met; the Stripe Connect onboarding/dashboard (Chrome Custom Tabs + `PayoutSetupScreen`) shipped earlier remains intact.  
- [x] `Profile/` — Complete — full edit user/carrier flows, avatar upload + delete, privacy preferences, disclaimer sync, account deletion + GDPR export, Settings screen with currency + clear-cache. All [20](20-profile-tab.md) menu deep-links wired.  
- [x] `Ratings/` — Complete — tabbed Feedback screen (received summary + cards, given with inline comment edit, pending list); rating submission flows through bookings (spec 05).  
- [x] `RouteActivity/` — Complete for current parity scope — summary endpoint + `RouteActivitySummaryViewModel` + carrier dashboard summary surface, `newPackagesThisWeekNearHome` surfaced on `RouteActivitySummary` (mapper still folds it into `totalTrips` for back-compat), `CarrierExploreDropdownState` ported (6 JVM tests covering iOS thresholds + focus/empty/section gating), and Recent / Popular dropdown overlay rendered under the carrier-explore search field. **Content-mode switching** ported: new `CarrierExploreContentMode` sealed class (`Nearby` / `Recent` / `PopularList` / `PopularDestination(name)` / `Search(query)`) + `CarrierBrowseContentMode` browse-mode mapping (iOS `browseModeFromExploreMode` parity — `PopularList` collapses to `Nearby` for the API call). Dropdown taps now swap the main content area: `PopularList` renders a `LazyColumn` of popular routes (using `PMenuRow`-style `PCard` rows with origin → destination + package count + chevron); tapping a route enters `PopularDestination(displayName)` which sets the search filter to that destination and shows a "Showing packages for X" header. `Recent` mode (iOS-parity, no backend probe needed): `PackageBrowseFilter` gains a `recent: Boolean` flag that — when true — injects `new_this_week=true&sort_by=created_at&sort_order=desc` into `toQueryMap()`, exactly matching iOS `CarrierBrowseContentMode.recent` in `CarrierHomeContent.swift:1824`. The Recent dropdown tap sets the flag + reapplies the filter, and `Back to Nearby` clears it. Recent mode renders the same package list as Nearby with a "Recent Packages Near You" header above. Every non-Nearby mode renders a "← Back to Nearby" affordance that returns + clears the search filter. 8 JVM tests in `CarrierExploreContentModeTest` covering all branches + `browseMode` mappings; 2 new tests in `PackageBrowseFilterTest` pin the recent-flag wire contract. 4 EN+FR strings (back-to-nearby, popular-routes title, showing-destination, recent-header). **Carrier-explore content-mode switching is full iOS parity** — no behavior delta remaining.  
- [x] `Shipper/` — Complete for current iOS-parity scope. `ShipperApi` (`GET /shipper/nearby-carriers`) + tolerant `KSerializer` for object-or-empty-array `data` shape + `ShipperRepository` + `NearbyCarriers` domain + `NearbyCarriersViewModel` (iOS-parity API: `loadNearbyCarriersIfNeeded` / `loadNearbyCarriers` / `resetForNewSession` + `carriersWithCompletedDeliveries`) + `NearbyCarriersSection` (horizontal `PChip` row, mounted in `ShipperExploreContent`). VM observes `AuthRepository.currentUser()` user-id transitions and auto-resets — equivalent to iOS `.onChange(of: roleViewModel.currentRole) resetForNewSession`; `drop(1)` skips the initial replay so first composition isn't a reset. Tap on a Top Carrier opens `UserProfilePopover` (carrier role). 7 JVM tests including the new auth-transition reset case. **Recent Searches** shipped as the iOS-parity sibling of Top Carriers: new `RecentSearchEntry` + `RecentSearchStore` (SharedPreferences, max 8, case-insensitive city+country dedupe) + `ShipperExploreSectionVisibility` pure helper (4-arg `shouldShowRecentSearches` mirroring iOS) + `ShipperRecentSearchesViewModel` + `RecentSearchesSection` (horizontal `PChip` row with `History` leading icon). `ShipperExploreContent` now renders Top Carriers OR Recent Searches via `when { showTopCarriers -> … ; showRecentSearches -> … }`. `PopularRoute` gained `destinationCountry: String?` — DTO already carried it; mapper now surfaces it. Saved on popular-route tap. 17 new JVM tests across `RecentSearchStoreTest` (8) and `ShipperExploreSectionVisibilityTest` (9). 1 new EN+FR string. **Behavior delta vs iOS** (intentional): saves only on popular-route tap, not on free-form search submit (search text isn't structured city+country on Android — adding a parser is a separate follow-up).  
- [x] `Support/` — Complete for current iOS-parity scope. `SupportApi` multipart `POST /support/tickets` + 9-category × 3-priority enums + `SupportTicketDraft` validation + `AttachmentReader` (`ContentResolver` + `OpenableColumns`) + `SupportTicketFormScreen` (category grid + filter chips + `PickVisualMedia` attachments + success dialog) + EN/FR strings. Deep-link entry points: Profile menu → Help Center → "Submit a Support Request" (HelpCenterSheet `onOpenSupportTicketForm`); booking match-details sheet → tertiary "Contact Support" button on `ShipperMatchDetailsSheetContent` (used by both shipper + carrier roles, plumbed via `MatchListScreen.onContactSupport` → `MainTabScreen` → `showSupportTicketFormSheet`). iOS opens `SupportTicketForm()` from three booking-detail surfaces (ShipperMatchDetailsView "Dispute Refund", CarrierMatchDetailsView CancellationReviewSection, shared BookingDetailsContentView "Contact Support") — Android collapses all three into the one shared sheet composable, so a single button covers them. Form has no match-id pre-fill on either platform. Label consolidated as "Contact Support" since the form is identical regardless of entry point.  
- [x] `Trips/` — Complete for current parity scope — includes My Trips detail/edit/cancel wiring, trip sheet route determinism coverage, and full trip-creation journey depth/integration slices from `03-trips.md`.  
- [x] `Verification/` — Complete — phone OTP flow (send/verify/resend/status, E.164 + 60s cooldown) and premium ID/selfie multipart with status surfacing.  

**Cross-cutting**

- [x] Shared networking stack in `:core:network` — Complete — interceptor policy + prod base URL.  
- [x] Design tokens in `:core:designsystem` — Complete.  
- [x] App shell / tabs / Explore — Complete — built in Phase 2 (`MainTabScreen`, role switcher, carrier + shipper explore); tab-specific feature depth continues in later phases.  
- [x] `google-services.json` + Firebase project linkage for FCM — Complete — linkage + runtime registration + push-tap routing wired end-to-end.  
- [x] Google / Facebook **sign-in flows** — Complete — Compose + backend done; Credential Manager / extra tests optional.
