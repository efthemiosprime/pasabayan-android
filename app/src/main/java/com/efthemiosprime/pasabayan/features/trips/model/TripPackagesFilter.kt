package com.efthemiosprime.pasabayan.features.trips.model

/** Client-only filter for carrier trip detail — package list tabs. */
enum class TripPackagesFilter {
    ALL,
    REMAINING,
    DELIVERED,
}
