<?xml version="1.0" encoding="utf-8"?>
<!--
  Cloud backup rules (API 31+ uses data_extraction_rules for finer control).
  Exclude auth tokens and sensitive prefs when those are added (Phase 1+).
  https://developer.android.com/guide/topics/data/autobackup
-->
<full-backup-content>
    <!-- When using encrypted token storage / DataStore, exclude here, e.g.:
    <exclude domain="sharedpref" path="auth.xml"/>
    <exclude domain="database" path="pasabayan.db"/>
    -->
</full-backup-content>
